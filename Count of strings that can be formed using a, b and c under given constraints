/***** Given a length n, count the number of strings of length n that can be made using ‘a’, ‘b’ and ‘c’ with at-most one ‘b’ and two ‘c’s
allowed********/

int main() 
{
	cin>>t;
	for(int i=0;i<t;i++)
	{
	    cin>>n;
	    x=(n*(n-1))/2;
	    ans=1+n+n+2*x+x+x*(n-2);
	    cout<<ans<<endl;
	}
	return 0;
}
